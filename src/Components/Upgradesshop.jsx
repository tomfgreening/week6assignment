export default function Upgradesshop(props) {
  return <p>{props.upgradesShopText}</p>;

//   export default function App() {
//     const items = ["Item 1", "Item 2", "Item 3"];
//     const htmlItems = items.map((item) => <li>{item}</li>);
  
//     return <ul>{htmlItems}</ul>;

// above code is from LISTS and KEYS workshop. Pickup from here tommorow morning.
  }
}
